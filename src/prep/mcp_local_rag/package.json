{
  "name": "mcp-local-rag",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP stdio server that exposes a local RAG HTTP API as tools for IDE assistants",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^1.0.0",
    "zod": "^4.0.0"
  }
}
