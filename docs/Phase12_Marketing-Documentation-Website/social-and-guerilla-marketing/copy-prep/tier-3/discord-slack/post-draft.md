# Post Draft for Discord/Slack (Showcase Channels)

## Target: Cursor Discord / Windsurf Discord

**Channel:** `#showcase` or `#built-with-cursor`

**Message:**
"Hey everyone! 👋

I built a free local MCP server called **CoDRAG** to help with context limits.
It builds a dependency graph of your code so you can fetch *only* the relevant functions/types for a task, instead of dumping full files.

It runs locally (Rust + Tauri).
Works great with `@codebase` struggles.

Repo: [Link]
(Screenshot attached: The dependency graph visualizer)"

## Target: LocalLLaMA Discord

**Channel:** `#projects`

**Message:**
"Built a local-first RAG engine for code that doesn't use vector embeddings.
Uses Tree-sitter + Graph traversal to find dependencies.
Runs locally, exposes via MCP.
[Link]"

## Tone
Casual, brief, community-member vibe.

## Timing
Weekend.
