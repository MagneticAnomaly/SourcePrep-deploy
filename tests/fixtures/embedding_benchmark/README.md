# Embedding Benchmark Fixture

A realistic mini-project used to benchmark embedding model quality for Prep.

Contains authentication, database, API, caching, and utility modules with
known query→answer ground truth pairs defined in `scripts/benchmark_embeddings.py`.
