# Prep Team Sync — RunPod Serverless Image
# Inherits from the GPU base image and adds the RunPod job handler.
#
# Build:
#   docker build -f runpod/Dockerfile.runpod -t my-org/prep-runpod .
#   docker push my-org/prep-runpod
#
# Then create a RunPod Serverless Endpoint using this image.

FROM ghcr.io/magneticanomaly/prep-headless:gpu

RUN pip install --no-cache-dir runpod

COPY runpod/runpod_handler.py /handler.py

CMD ["python", "-u", "/handler.py"]
