{
  "_comment": "Prep Headless — AWS ECS Task Definition (reference). Adjust family, executionRoleArn, and environment values for your VPC.",
  "family": "prep-headless-sync",
  "requiresCompatibilities": ["EC2"],
  "networkMode": "awsvpc",
  "cpu": "4096",
  "memory": "16384",
  "executionRoleArn": "arn:aws:iam::YOUR_ACCOUNT:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::YOUR_ACCOUNT:role/prepHeadlessRole",
  "containerDefinitions": [
    {
      "name": "prep-headless",
      "image": "ghcr.io/magneticanomaly/prep-headless:gpu",
      "essential": true,
      "command": [
        "sync-headless",
        "--repo-path", "/workspace",
        "--branch", "main",
        "--model-provider", "local",
        "--model-name", "qwen3:4b",
        "--embedder", "native"
      ],
      "environment": [
        { "name": "PREP_S3_BUCKET", "value": "my-internal-prep-indexes" },
        { "name": "PREP_S3_PREFIX", "value": "my-repo/main" }
      ],
      "secrets": [
        { "name": "PREP_S3_ACCESS_KEY", "valueFrom": "arn:aws:secretsmanager:us-east-1:YOUR_ACCOUNT:secret:prep/s3-access-key" },
        { "name": "PREP_S3_SECRET_KEY", "valueFrom": "arn:aws:secretsmanager:us-east-1:YOUR_ACCOUNT:secret:prep/s3-secret-key" }
      ],
      "resourceRequirements": [
        { "type": "GPU", "value": "1" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/prep-headless",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "sync"
        }
      },
      "mountPoints": [
        { "sourceVolume": "workspace", "containerPath": "/workspace" }
      ]
    }
  ],
  "volumes": [
    { "name": "workspace", "host": {} }
  ]
}
