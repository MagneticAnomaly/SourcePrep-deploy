# clara-server configuration
# Copy this to .env and customize as needed

# Model settings
CLARA_MODEL=apple/CLaRa-7B-Instruct
CLARA_SUBFOLDER=compression-16
CLARA_CACHE=~/.cache/clara-server

# Server settings
CLARA_HOST=0.0.0.0
CLARA_PORT=8765

# Backend: auto, cuda, mps, mlx, cpu
CLARA_BACKEND=auto

# Optional: API key authentication
# CLARA_API_KEY=your-secret-key

# Optional: Rate limiting (requests per minute per IP)
# CLARA_RATE_LIMIT=60

# Logging level: DEBUG, INFO, WARNING, ERROR
CLARA_LOG_LEVEL=INFO
